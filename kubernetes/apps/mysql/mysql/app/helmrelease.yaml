---
apiVersion: helm.toolkit.fluxcd.io/v2beta2
kind: HelmRelease
metadata:
  name: mysql
  namespace: db-system
spec:
  interval: 30m
  chart:
    spec:
      chart: mysql
      version: "*"
      sourceRef:
        kind: HelmRepository
        name: bitnami
      interval: 12h
  install:
    remediation:
      retries: 3
  upgrade:
    cleanupOnFail: true
    remediation:
      retries: 3
  valuesFrom:
    - kind: ConfigMap
      name: mysql-helm-values
